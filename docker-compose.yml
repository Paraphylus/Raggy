services:
  rag-chatbot:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      GROQ_API_KEY: ${GROQ_API_KEY}
    volumes:
      - ./data:/app/data
